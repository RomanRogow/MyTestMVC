<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.9.xsd">

    <changeSet id="003-fill-created-at" author="system">
        <!-- Заполняем created_at для ВСЕХ записей, где NULL -->
        <sql>
            UPDATE employees
            SET created_at = CURRENT_TIMESTAMP
            WHERE created_at IS NULL;
        </sql>

        <sql>
            UPDATE employees
            SET updated_at = CURRENT_TIMESTAMP
            WHERE updated_at IS NULL;
        </sql>

        <comment>Заполняем created_at и updated_at для существующих записей</comment>
    </changeSet>

    <changeSet id="004-fill-personal-codes" author="system">
        <!-- Заполняем personal_code для ВСЕХ записей, где NULL -->
        <sql>
            UPDATE employees
            SET personal_code = CONCAT('EMP-', id, '-',
                                       EXTRACT(EPOCH FROM CURRENT_TIMESTAMP)::BIGINT % 10000)
            WHERE personal_code IS NULL;
        </sql>

        <comment>Генерируем personal_code для существующих записей</comment>
    </changeSet>

</databaseChangeLog>